@model IEnumerable<ProjetHotel.Models.Paiement>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Prix)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DatePaiment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Méthode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Satut)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RéservationId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Prix)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DatePaiment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Méthode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Satut)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RéservationId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
